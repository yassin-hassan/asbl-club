package club.asbl.asbl_club.membership;

/** A member's category within an association. Mirrors the {@code memberships.category} check constraint. */
public enum MembershipCategory {
    FULL, SUPPORTER, HONORARY
}
