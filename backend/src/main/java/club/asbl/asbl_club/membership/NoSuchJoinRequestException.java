package club.asbl.asbl_club.membership;

// No pending request from that person in that association (never asked, already handled, or another association).
public class NoSuchJoinRequestException extends RuntimeException {
}
