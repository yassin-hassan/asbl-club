package club.asbl.asbl_club.membership;

/** A member's role within an association. Mirrors the {@code memberships.role} check constraint. */
public enum MembershipRole {
    ADMIN, TREASURER, VIEWER, MEMBER
}
